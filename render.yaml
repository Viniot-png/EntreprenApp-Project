services:
  - type: web
    name: entreprenapp-backend
    env: node
    plan: standard
    buildCommand: bash build-production.sh
    startCommand: node server-unified.js
    envVars:
      - key: NODE_ENV
        value: production

